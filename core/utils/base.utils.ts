export const isArray = Array.isArray;
export const keyList = Object.keys;
export const hasProp = Object.prototype.hasOwnProperty;

export type checkerResponse = {
  success: boolean;
  message?: string;
};
